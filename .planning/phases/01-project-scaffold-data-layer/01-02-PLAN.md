---
phase: 01-project-scaffold-data-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - event-service/src/main/resources/db/migration/V1__create_events_table.sql
  - event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java
  - event-service/src/main/java/com/example/ticketbooking/event/entity/EventCategory.java
  - event-service/src/main/java/com/example/ticketbooking/event/entity/EventStatus.java
  - event-service/src/main/java/com/example/ticketbooking/event/repository/EventRepository.java
  - event-service/src/main/java/com/example/ticketbooking/event/dto/EventDto.java
  - event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java
  - event-service/src/test/java/com/example/ticketbooking/event/EventDataLayerIT.java
  - event-service/pom.xml
autonomous: true
requirements:
  - INFR-02
  - INFR-04
user_setup: []

must_haves:
  truths:
    - "Starting event-service connects to PostgreSQL and Flyway applies the V1 events-table migration automatically"
    - "Hibernate ddl-auto=validate passes — the Event entity exactly matches the Flyway-created events table"
    - "MapStruct generates EventMapper implementation at compile time with no hand-written mapping code"
    - "The Event entity uses a UUID primary key and includes name, description, category, venue, city, event date/time, ticket counts, price, and status"
    - "Category is a Java enum with CONCERT, SPORTS, THEATER, CONFERENCE, FESTIVAL, EXHIBITION; status is UPCOMING, ONGOING, COMPLETED, CANCELLED; both stored as STRING"
    - "Event entity maintains created_at and updated_at via @PrePersist / @PreUpdate lifecycle callbacks"
  artifacts:
    - path: "event-service/src/main/resources/db/migration/V1__create_events_table.sql"
      provides: "Flyway migration creating the events table with UUID PK and audit columns"
      contains: "CREATE TABLE events"
    - path: "event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java"
      provides: "JPA entity mapping the events table"
      contains: "GenerationType.UUID"
      min_lines: 40
    - path: "event-service/src/main/java/com/example/ticketbooking/event/entity/EventCategory.java"
      provides: "Category enum (D-01, D-02)"
      contains: "EXHIBITION"
    - path: "event-service/src/main/java/com/example/ticketbooking/event/entity/EventStatus.java"
      provides: "Status enum"
      contains: "CANCELLED"
    - path: "event-service/src/main/java/com/example/ticketbooking/event/repository/EventRepository.java"
      provides: "Spring Data JPA repository for Event"
      contains: "JpaRepository<Event, UUID>"
    - path: "event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java"
      provides: "MapStruct mapper interface (Spring component model)"
      contains: "componentModel"
    - path: "event-service/src/main/java/com/example/ticketbooking/event/dto/EventDto.java"
      provides: "Response DTO so entities are never exposed (INFR-04)"
  key_links:
    - from: "event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java"
      to: "event-service/src/main/resources/db/migration/V1__create_events_table.sql"
      via: "JPA @Column names and types must match the migration columns for ddl-auto=validate"
      pattern: "@Table\\(name = \"events\"\\)"
    - from: "event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java"
      to: "event-service/src/main/java/com/example/ticketbooking/event/dto/EventDto.java"
      via: "MapStruct generates Event<->EventDto mapping from matching field names"
      pattern: "EventDto toDto\\(Event"
    - from: "event-service/src/main/java/com/example/ticketbooking/event/repository/EventRepository.java"
      to: "event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java"
      via: "JpaRepository typed on Event and UUID"
      pattern: "JpaRepository<Event, UUID>"
---

<objective>
Build the verified data layer on top of the Plan 01 scaffold: the Flyway V1 migration, the Event JPA entity (UUID PK + audit timestamps), the EventCategory and EventStatus enums, the Spring Data repository, the response DTO, and the MapStruct mapper. Prove the whole layer end-to-end with a Testcontainers integration test where Flyway applies the migration and Hibernate's `ddl-auto=validate` confirms entity/table alignment. This satisfies INFR-02 (Flyway-managed schema) and INFR-04 (MapStruct DTO mapping).

Purpose: This is the heart of the phase goal — a working database layer where entities, migrations, repositories, and DTO mapping are all wired and verified. The entity must match the migration exactly or `ddl-auto=validate` fails at startup (RESEARCH.md Pitfall 3).

Output: A persistable Event entity, an auto-applied migration, a typed repository, and a compile-time-generated mapper, all verified by an integration test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-data-layer/01-CONTEXT.md
@.planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md
@.claude/CLAUDE.md
@.planning/phases/01-project-scaffold-data-layer/01-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files:
- `event-service/src/main/resources/db/migration/V1__create_events_table.sql` — Flyway migration
- `event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java` — entity class `Event`
- `event-service/src/main/java/com/example/ticketbooking/event/entity/EventCategory.java` — enum `EventCategory`
- `event-service/src/main/java/com/example/ticketbooking/event/entity/EventStatus.java` — enum `EventStatus`
- `event-service/src/main/java/com/example/ticketbooking/event/repository/EventRepository.java` — interface `EventRepository`
- `event-service/src/main/java/com/example/ticketbooking/event/dto/EventDto.java` — DTO `EventDto`
- `event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java` — interface `EventMapper`
- `event-service/src/test/java/com/example/ticketbooking/event/EventDataLayerIT.java` — integration test `EventDataLayerIT`

New symbols:
- Class `Event` (fields: `id:UUID`, `name:String`, `description:String`, `category:EventCategory`, `venue:String`, `city:String`, `eventDateTime:LocalDateTime`, `totalTickets:Integer`, `availableTickets:Integer`, `price:BigDecimal`, `status:EventStatus`, `createdAt:LocalDateTime`, `updatedAt:LocalDateTime`); methods `onCreate()` (@PrePersist), `onUpdate()` (@PreUpdate)
- Enum `EventCategory` constants: CONCERT, SPORTS, THEATER, CONFERENCE, FESTIVAL, EXHIBITION
- Enum `EventStatus` constants: UPCOMING, ONGOING, COMPLETED, CANCELLED
- Interface `EventRepository extends JpaRepository<Event, UUID>`
- Class/record `EventDto` (mirrors Event fields)
- Interface `EventMapper` methods: `EventDto toDto(Event event)`, `Event toEntity(EventDto dto)`
- Test class `EventDataLayerIT`
- DB table `events`; Maven test dependency `org.testcontainers:postgresql` (added to event-service/pom.xml)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Flyway V1 migration, Event entity, and enums</name>
  <read_first>
    - .planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md ("Flyway SQL Migration" Pattern 4, "Complete Event Entity", "EventCategory Enum", "EventStatus Enum", Pitfalls 3 and 5)
    - .planning/phases/01-project-scaffold-data-layer/01-CONTEXT.md (decisions D-01, D-02, D-03, D-04)
    - event-service/src/main/resources/application.yml (Flyway locations and ddl-auto setting from Plan 01)
  </read_first>
  <files>event-service/src/main/resources/db/migration/V1__create_events_table.sql, event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java, event-service/src/main/java/com/example/ticketbooking/event/entity/EventCategory.java, event-service/src/main/java/com/example/ticketbooking/event/entity/EventStatus.java</files>
  <behavior>
    - Flyway applies V1 on startup, creating the events table with a native PostgreSQL `UUID` PK column.
    - Hibernate ddl-auto=validate passes: every @Column on Event maps to an identically-typed migration column.
    - Persisting a new Event with a null id auto-generates a UUID and sets created_at/updated_at.
    - Re-saving an existing Event updates updated_at but leaves created_at unchanged.
    - Category and status round-trip as readable strings in the DB (not ordinals).
  </behavior>
  <action>
    Create `V1__create_events_table.sql` under `event-service/src/main/resources/db/migration/` per RESEARCH.md Pattern 4: a `CREATE TABLE events` with column `id` of PostgreSQL native type `UUID` as PRIMARY KEY (use native `UUID`, not VARCHAR — Pitfall 3), plus `name VARCHAR(255) NOT NULL`, `description TEXT`, `category VARCHAR(50) NOT NULL`, `venue VARCHAR(255) NOT NULL`, `city VARCHAR(255) NOT NULL`, `event_date_time TIMESTAMP NOT NULL`, `total_tickets INTEGER NOT NULL`, `available_tickets INTEGER NOT NULL`, `price NUMERIC(10,2) NOT NULL`, `status VARCHAR(50) NOT NULL`, `created_at TIMESTAMP NOT NULL`, `updated_at TIMESTAMP NOT NULL`.
    Create the `Event` entity in package `com.example.ticketbooking.event.entity` per RESEARCH.md "Complete Event Entity": `@Entity @Table(name = "events")` with Lombok `@Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder`. PK uses `@Id @GeneratedValue(strategy = GenerationType.UUID)` on a `UUID id` field (D — UUID PK). Map every column with matching `@Column(name = ...)` names from the migration. `category` and `status` fields use `@Enumerated(EnumType.STRING)` (Pitfall 5 — never ordinal). Add `createdAt`/`updatedAt` `LocalDateTime` fields with `@PrePersist onCreate()` setting both and `@PreUpdate onUpdate()` setting only updatedAt (D-03, D-04 — lifecycle callbacks, NOT Spring Data Auditing). Use `BigDecimal price`, `LocalDateTime eventDateTime`, `Integer totalTickets`/`availableTickets` per Claude's discretion field types.
    Create `EventCategory` enum in `...event.entity` with exactly: CONCERT, SPORTS, THEATER, CONFERENCE, FESTIVAL, EXHIBITION (D-01, D-02).
    Create `EventStatus` enum in `...event.entity` with exactly: UPCOMING, ONGOING, COMPLETED, CANCELLED.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE events" event-service/src/main/resources/db/migration/V1__create_events_table.sql && grep -q "GenerationType.UUID" event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java && grep -q "EnumType.STRING" event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java && grep -q "EXHIBITION" event-service/src/main/java/com/example/ticketbooking/event/entity/EventCategory.java && grep -q "CANCELLED" event-service/src/main/java/com/example/ticketbooking/event/entity/EventStatus.java</automated>
  </verify>
  <acceptance_criteria>
    - `V1__create_events_table.sql` contains `CREATE TABLE events` and declares `id` with PostgreSQL native `UUID` type as PRIMARY KEY.
    - Migration includes all columns: name, description, category, venue, city, event_date_time, total_tickets, available_tickets, price (NUMERIC(10,2)), status, created_at, updated_at.
    - `Event.java` uses `@GeneratedValue(strategy = GenerationType.UUID)` and `@Table(name = "events")`.
    - `Event.java` annotates both category and status with `@Enumerated(EnumType.STRING)`.
    - `Event.java` has `@PrePersist` and `@PreUpdate` callback methods setting the audit timestamps; it does NOT use `@EnableJpaAuditing` or `@CreatedDate` (`grep -c 'CreatedDate\|EnableJpaAuditing' Event.java` returns 0).
    - `EventCategory.java` lists exactly the six constants; `EventStatus.java` lists exactly the four constants.
  </acceptance_criteria>
  <done>Migration, entity, and both enums exist with UUID PK, STRING-stored enums, lifecycle-callback audit timestamps, and column/field alignment ready for ddl-auto=validate.</done>
</task>

<task type="auto">
  <name>Task 2: Create repository, DTO, and MapStruct mapper</name>
  <read_first>
    - .planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md ("EventRepository", "EventMapper", Pattern 3 MapStruct Spring component model, Pitfall 1 processor ordering)
    - event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java (entity field names the DTO and mapper mirror)
    - event-service/src/main/java/com/example/ticketbooking/event/entity/EventCategory.java
    - event-service/src/main/java/com/example/ticketbooking/event/entity/EventStatus.java
  </read_first>
  <files>event-service/src/main/java/com/example/ticketbooking/event/repository/EventRepository.java, event-service/src/main/java/com/example/ticketbooking/event/dto/EventDto.java, event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java</files>
  <action>
    Create `EventRepository` in package `com.example.ticketbooking.event.repository` as an interface extending `JpaRepository<Event, UUID>` (no custom methods this phase).
    Create `EventDto` in package `com.example.ticketbooking.event.dto` mirroring the Event fields with identical names so MapStruct auto-maps by name: id (UUID), name, description, category (EventCategory), venue, city, eventDateTime (LocalDateTime), totalTickets (Integer), availableTickets (Integer), price (BigDecimal), status (EventStatus), createdAt, updatedAt (LocalDateTime). Use a Java record or a Lombok-annotated class — entities must never be exposed in API responses (INFR-04), so this DTO is the API-facing type.
    Create `EventMapper` in package `com.example.ticketbooking.event.mapper` per RESEARCH.md Pattern 3: `@Mapper(componentModel = MappingConstants.ComponentModel.SPRING)` interface with `EventDto toDto(Event event)` and `Event toEntity(EventDto dto)`. Do NOT hand-write mapping bodies — MapStruct generates the implementation at compile time. The generated impl appears under `target/generated-sources` after build.
  </action>
  <verify>
    <automated>grep -q "JpaRepository<Event, UUID>" event-service/src/main/java/com/example/ticketbooking/event/repository/EventRepository.java && grep -q "componentModel" event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java && grep -q "EventDto toDto" event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java</automated>
  </verify>
  <acceptance_criteria>
    - `EventRepository.java` is an interface extending `JpaRepository<Event, UUID>`.
    - `EventDto` declares the same field names as Event so MapStruct maps without explicit `@Mapping` annotations.
    - `EventMapper.java` is annotated `@Mapper` with Spring component model and declares both `toDto(Event)` and `toEntity(EventDto)`.
    - `EventMapper.java` contains no hand-written method bodies (interface methods only — `grep -c 'return ' EventMapper.java` returns 0).
  </acceptance_criteria>
  <done>Repository, DTO, and MapStruct mapper interface exist; mapper relies on compile-time generation with Spring component model.</done>
</task>

<task type="auto">
  <name>Task 3: Add Testcontainers and verify full data layer end-to-end</name>
  <read_first>
    - .planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md (Testing stack note: Testcontainers + @ServiceConnection, Pitfall 2 Flyway modules, Pitfall 3 UUID type, Environment Availability)
    - .claude/CLAUDE.md (Testing table: Testcontainers, @ServiceConnection, MockMvc)
    - event-service/pom.xml (existing dependencies — add Testcontainers test deps here)
    - event-service/src/main/java/com/example/ticketbooking/event/entity/Event.java
    - event-service/src/main/java/com/example/ticketbooking/event/mapper/EventMapper.java
  </read_first>
  <files>event-service/pom.xml, event-service/src/test/java/com/example/ticketbooking/event/EventDataLayerIT.java</files>
  <action>
    Add Testcontainers test dependencies to `event-service/pom.xml`: `org.springframework.boot:spring-boot-testcontainers` and `org.testcontainers:postgresql`, both `<scope>test</scope>` (versions managed by the Boot BOM). Do NOT pin explicit Testcontainers versions — let the Boot 3.3.13 BOM manage them.
    Create `EventDataLayerIT` integration test in package `com.example.ticketbooking.event` using `@SpringBootTest` and a PostgreSQL Testcontainer wired via `@ServiceConnection` (Boot 3.1+ pattern, per CLAUDE.md Testing). The test must prove the four phase truths:
    1. Context loads, Flyway applies V1 automatically, and Hibernate `ddl-auto=validate` passes (the entity matches the migrated table — if columns mismatch the context fails to start, failing the test). This directly exercises Pitfall 3 (UUID type) and Pitfall 2 (Flyway postgres module).
    2. Saving a new `Event` (built via the Lombok builder with category=CONCERT, status=UPCOMING) auto-generates a non-null UUID id and non-null createdAt/updatedAt.
    3. Injecting `EventMapper` (Spring bean) and calling `toDto(savedEvent)` returns an `EventDto` whose fields equal the entity's (id, name, category, status), proving compile-time generation succeeded and the bean is wired.
    4. Reloading via `EventRepository.findById` returns the persisted event with category/status as the original enum values (proving STRING storage round-trips).
    Run the full build with tests: `./mvnw clean install` (or IntelliJ-bundled mvn) must pass — this is the build-proof gate that was deferred from Plan 01 Task 3.
  </action>
  <verify>
    <automated>grep -q "testcontainers" event-service/pom.xml && grep -q "@ServiceConnection" event-service/src/test/java/com/example/ticketbooking/event/EventDataLayerIT.java && ./mvnw clean install -q</automated>
  </verify>
  <acceptance_criteria>
    - `event-service/pom.xml` contains `spring-boot-testcontainers` and `org.testcontainers:postgresql` with test scope.
    - `EventDataLayerIT.java` uses `@SpringBootTest` and `@ServiceConnection` with a PostgreSQL container.
    - The test asserts: saved Event has a non-null UUID id and non-null createdAt/updatedAt; `EventMapper.toDto` returns a matching `EventDto`; `findById` round-trips category and status as enum values.
    - `./mvnw clean install` exits 0 with all tests passing — Flyway applies V1 and ddl-auto=validate confirms entity/table alignment (verifies ROADMAP Success Criteria 1, 2, 3, 4).
  </acceptance_criteria>
  <done>Testcontainers integration test passes, proving Flyway auto-migration, ddl-auto=validate alignment, UUID PK generation, audit timestamps, STRING enum storage, and compile-time MapStruct mapping. Full `mvn clean install` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → database | Entity persistence and Flyway migrations write to PostgreSQL |
| repository → SQL | Spring Data JPA generates parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Flyway applied migrations | mitigate | Flyway verifies migration checksums on each run; V1 must never be edited after it is applied — future schema changes go in V2+ |
| T-02-02 | Tampering | Repository SQL injection | mitigate | EventRepository uses Spring Data JPA derived/parameterized queries only; no raw SQL string concatenation in the data layer |
| T-02-03 | Information Disclosure | Entity exposure in API | mitigate | EventDto is the API-facing type (INFR-04); the Event entity is never serialized directly, preventing leak of internal mapping/audit internals |
</threat_model>

<verification>
- `./mvnw clean install` passes with the Testcontainers integration test green.
- Flyway applies V1 automatically at context startup; ddl-auto=validate confirms entity/table alignment.
- MapStruct generates the EventMapper implementation under target/generated-sources (no hand-written mapping).
- Event uses UUID PK, STRING-stored enums, and @PrePersist/@PreUpdate audit timestamps.
</verification>

<success_criteria>
- ROADMAP Success Criterion 2: Flyway applies the event table migration automatically on startup.
- ROADMAP Success Criterion 3: MapStruct generates DTO/entity mappers at compile time with no manual mapping code.
- ROADMAP Success Criterion 4: Event entity uses UUID PK and includes all required fields and status.
- INFR-02 satisfied: schema managed by Flyway (ddl-auto=validate, not ddl-auto generation).
- INFR-04 satisfied: MapStruct DTO mapping with EventDto as the API-facing type.
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold-data-layer/01-02-SUMMARY.md` when done.
</output>
