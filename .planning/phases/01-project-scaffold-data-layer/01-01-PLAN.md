---
phase: 01-project-scaffold-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - mvnw
  - mvnw.cmd
  - .mvn/wrapper/maven-wrapper.properties
  - docker-compose.yml
  - event-service/pom.xml
  - event-service/src/main/java/com/example/ticketbooking/event/EventServiceApplication.java
  - event-service/src/main/resources/application.yml
  - event-service/src/test/java/com/example/ticketbooking/event/EventServiceApplicationTests.java
autonomous: true
requirements:
  - INFR-01
user_setup: []

must_haves:
  truths:
    - "Running mvn clean install from the project root compiles the parent POM and event-service module without errors"
    - "The parent POM uses pom packaging and lists event-service as a module"
    - "Only event-service carries spring-boot-maven-plugin (not the parent)"
    - "The annotation processor path order is Lombok, then mapstruct-processor, then lombok-mapstruct-binding"
    - "The Maven wrapper exists at the project root and runs from any terminal without a global Maven install"
  artifacts:
    - path: "pom.xml"
      provides: "Parent POM with dependencyManagement and annotation processor pluginManagement"
      contains: "<packaging>pom</packaging>"
    - path: "event-service/pom.xml"
      provides: "Bootable module POM with spring-boot-maven-plugin and all data-layer dependencies"
      contains: "spring-boot-maven-plugin"
    - path: "event-service/src/main/java/com/example/ticketbooking/event/EventServiceApplication.java"
      provides: "Spring Boot entrypoint"
      contains: "@SpringBootApplication"
    - path: "event-service/src/main/resources/application.yml"
      provides: "Datasource, JPA validate, Flyway, server.port=8081 configuration"
      contains: "8081"
    - path: "mvnw.cmd"
      provides: "Maven wrapper for Windows builds without global Maven"
  key_links:
    - from: "pom.xml"
      to: "event-service/pom.xml"
      via: "parent POM <modules> lists event-service; module POM <parent> points back"
      pattern: "<module>event-service</module>"
    - from: "event-service/pom.xml"
      to: "pom.xml"
      via: "module inherits annotation processor config and dependencyManagement from parent"
      pattern: "<artifactId>ticket-booking-backend</artifactId>"
---

<objective>
Establish the multi-module Maven scaffold for the ticket-booking backend: a parent aggregator POM, a bootable event-service module, a Maven wrapper at the project root, and a minimal Spring Boot application that compiles and whose context loads against PostgreSQL. This satisfies INFR-01 (multi-module Maven structure with parent POM and event-service module).

Purpose: Every subsequent task in this phase and the entire project depends on a buildable module. Getting the POM structure, annotation processor ordering, and wrapper placement right here prevents the compile-time failures documented in RESEARCH.md.

Output: A project that compiles with `mvn clean install` and a Spring Boot application class. No entities, migrations, or mappers yet — those land in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-data-layer/01-CONTEXT.md
@.planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files (none pre-exist; greenfield):
- `pom.xml` — parent aggregator POM (packaging `pom`)
- `mvnw`, `mvnw.cmd`, `.mvn/wrapper/maven-wrapper.properties` — Maven wrapper at project root
- `docker-compose.yml` — local PostgreSQL service
- `event-service/pom.xml` — bootable module POM
- `event-service/src/main/java/com/example/ticketbooking/event/EventServiceApplication.java` — `@SpringBootApplication` class `EventServiceApplication` with `main` method
- `event-service/src/main/resources/application.yml` — app configuration
- `event-service/src/test/java/com/example/ticketbooking/event/EventServiceApplicationTests.java` — context-load test class `EventServiceApplicationTests`

New symbols:
- Class `com.example.ticketbooking.event.EventServiceApplication`
- Test class `com.example.ticketbooking.event.EventServiceApplicationTests`
- Maven group `com.example`, artifact `ticket-booking-backend`, version `0.0.1-SNAPSHOT`
- Maven module artifact `event-service`
- Maven properties: `java.version=21`, `mapstruct.version=1.6.3`, `lombok-mapstruct-binding.version=0.2.0`
- Docker compose service `postgres` exposing database `ticketbooking`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create parent POM and Maven wrapper at project root</name>
  <read_first>
    - .planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md (Pattern 1, "Complete Parent POM" code example, Pitfalls 4 and 6)
    - .claude/CLAUDE.md (Technology Stack version matrix, "What NOT to Use")
  </read_first>
  <files>pom.xml, mvnw, mvnw.cmd, .mvn/wrapper/maven-wrapper.properties</files>
  <action>
    Create the parent POM at the project root `pom.xml` exactly per the RESEARCH.md "Complete Parent POM" example:
    - `<parent>` is `org.springframework.boot:spring-boot-starter-parent:3.3.13` with empty `<relativePath/>`.
    - Project coordinates: groupId `com.example`, artifactId `ticket-booking-backend`, version `0.0.1-SNAPSHOT`, `<packaging>pom</packaging>`.
    - `<modules>` contains a single `<module>event-service</module>`.
    - `<properties>`: `java.version=21`, `mapstruct.version=1.6.3`, `lombok-mapstruct-binding.version=0.2.0`.
    - `<dependencyManagement>` declares `org.mapstruct:mapstruct` pinned to `${mapstruct.version}`.
    - `<build><pluginManagement>` configures `maven-compiler-plugin` with `<annotationProcessorPaths>` in this exact order: (1) `org.projectlombok:lombok` version `${lombok.version}`, (2) `org.mapstruct:mapstruct-processor` version `${mapstruct.version}`, (3) `org.projectlombok:lombok-mapstruct-binding` version `${lombok-mapstruct-binding.version}`. Order is load-bearing per Pitfall 1 — Lombok must precede MapStruct.
    - Do NOT place `spring-boot-maven-plugin` in the parent POM (Pitfall 4).
    Generate the Maven wrapper files (`mvnw`, `mvnw.cmd`, `.mvn/wrapper/maven-wrapper.properties`) at the PROJECT ROOT, not inside event-service (Pitfall 6). Maven is not on the system PATH; the wrapper is invoked via IntelliJ-bundled Maven at `C:/Program Files/JetBrains/IntelliJ IDEA 2024.3.5/plugins/maven/lib/maven3/bin/mvn` to bootstrap wrapper generation, or by writing the standard `maven-wrapper.properties` pointing at distributionUrl for Maven 3.9.x. The wrapper must run `./mvnw` from the project root.
  </action>
  <verify>
    <automated>test -f pom.xml && test -f mvnw.cmd && test -f .mvn/wrapper/maven-wrapper.properties && grep -q "<packaging>pom</packaging>" pom.xml && grep -q "<module>event-service</module>" pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` exists at project root and contains `<packaging>pom</packaging>`.
    - `pom.xml` contains `<module>event-service</module>`.
    - `pom.xml` declares the three annotation processor paths with lombok appearing on an earlier line than mapstruct-processor (verify ordering: `grep -n 'lombok</artifactId>\|mapstruct-processor</artifactId>' pom.xml` shows lombok before mapstruct-processor).
    - `pom.xml` does NOT contain `spring-boot-maven-plugin` (`grep -c 'spring-boot-maven-plugin' pom.xml` returns 0).
    - `mvnw.cmd` and `.mvn/wrapper/maven-wrapper.properties` exist at the project root.
  </acceptance_criteria>
  <done>Parent POM with pom packaging, single event-service module, correct annotation processor ordering, and no spring-boot-maven-plugin; Maven wrapper present at project root.</done>
</task>

<task type="auto">
  <name>Task 2: Create event-service module POM with data-layer dependencies</name>
  <read_first>
    - .planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md ("Complete Event-Service Module POM" code example, Standard Stack table)
    - pom.xml (the parent POM created in Task 1 — module must reference it as parent)
  </read_first>
  <files>event-service/pom.xml</files>
  <action>
    Create `event-service/pom.xml` per the RESEARCH.md "Complete Event-Service Module POM" example:
    - `<parent>` references `com.example:ticket-booking-backend:0.0.1-SNAPSHOT` (no version overrides; inherits from parent).
    - `<artifactId>event-service</artifactId>`.
    - Dependencies (versions managed by Boot BOM / parent, declare without explicit version unless noted):
      `spring-boot-starter-data-jpa`, `spring-boot-starter-web`, `org.flywaydb:flyway-core`, `org.flywaydb:flyway-database-postgresql` (both required per Pitfall 2), `org.postgresql:postgresql` with `<scope>runtime</scope>`, `org.mapstruct:mapstruct`, `org.projectlombok:lombok` with `<optional>true</optional>`, and `spring-boot-starter-test` with `<scope>test</scope>`.
    - `<build><plugins>` includes `spring-boot-maven-plugin` (ONLY here, not parent) with a Lombok `<exclude>` in its configuration.
    Do NOT add Spring WebFlux, Kafka, or GraphQL (CLAUDE.md "What NOT to Use").
  </action>
  <verify>
    <automated>test -f event-service/pom.xml && grep -q "spring-boot-maven-plugin" event-service/pom.xml && grep -q "flyway-database-postgresql" event-service/pom.xml && grep -q "<artifactId>ticket-booking-backend</artifactId>" event-service/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `event-service/pom.xml` exists and references parent artifactId `ticket-booking-backend`.
    - Contains both `flyway-core` and `flyway-database-postgresql` dependencies.
    - Contains `spring-boot-starter-data-jpa`, `spring-boot-starter-web`, `org.postgresql:postgresql`, `org.mapstruct:mapstruct`, `org.projectlombok:lombok`, and `spring-boot-starter-test`.
    - Contains `spring-boot-maven-plugin` in the build plugins section.
    - `lombok` dependency is marked `<optional>true</optional>`.
  </acceptance_criteria>
  <done>Module POM inherits from parent, declares all data-layer dependencies including both Flyway modules, and is the only POM carrying spring-boot-maven-plugin.</done>
</task>

<task type="auto">
  <name>Task 3: Create Spring Boot application, configuration, docker-compose, and verify build</name>
  <read_first>
    - .planning/phases/01-project-scaffold-data-layer/01-RESEARCH.md ("application.yml Configuration" example, Recommended Project Structure, Environment Availability / docker note, Anti-Patterns ddl-auto)
    - event-service/pom.xml (dependencies available to the application)
  </read_first>
  <files>event-service/src/main/java/com/example/ticketbooking/event/EventServiceApplication.java, event-service/src/main/resources/application.yml, event-service/src/test/java/com/example/ticketbooking/event/EventServiceApplicationTests.java, docker-compose.yml</files>
  <action>
    Create the Spring Boot entrypoint `EventServiceApplication.java` in package `com.example.ticketbooking.event`: a public class annotated `@SpringBootApplication` with a standard `public static void main(String[] args)` calling `SpringApplication.run(EventServiceApplication.class, args)`.
    Create `application.yml` per the RESEARCH.md example: `server.port: 8081`; `spring.application.name: event-service`; datasource url `jdbc:postgresql://localhost:5432/ticketbooking` with username/password from env vars `${DB_USERNAME:testuser}` / `${DB_PASSWORD:testpass}` and `driver-class-name: org.postgresql.Driver`; `spring.jpa.hibernate.ddl-auto: validate` (NOT update/create — Anti-Patterns, Flyway owns schema); `spring.flyway.enabled: true` with `locations: classpath:db/migration` and `baseline-on-migrate: true`.
    Create `docker-compose.yml` at the project root defining a `postgres` service (postgres:16 image) exposing port 5432 with database `ticketbooking`, user `testuser`, password `testpass` — this resolves RESEARCH.md Open Question 3 (reproducible DB) using the dedicated `ticketbooking` database (Open Question 1).
    Create `EventServiceApplicationTests.java` as a `@SpringBootTest` context-load test. Since there is no Flyway migration yet (Plan 02), this test must not require a real database connection in this plan — annotate it so the context loads without a live datasource, OR scope its assertion to a trivial `contextLoads` that is allowed to be excluded from `mvn clean install` if no DB is available. Prefer making `mvn clean install -DskipTests` the build-proof gate for THIS plan and keep `contextLoads` as a smoke test for Plan 02 once the schema exists. Document this choice in the SUMMARY.
    Run the build to prove compilation: `./mvnw clean install -DskipTests` (or the IntelliJ-bundled mvn) must succeed for both modules.
  </action>
  <verify>
    <automated>test -f event-service/src/main/java/com/example/ticketbooking/event/EventServiceApplication.java && grep -q "@SpringBootApplication" event-service/src/main/java/com/example/ticketbooking/event/EventServiceApplication.java && grep -q "8081" event-service/src/main/resources/application.yml && grep -q "validate" event-service/src/main/resources/application.yml && ./mvnw clean install -DskipTests -q</automated>
  </verify>
  <acceptance_criteria>
    - `EventServiceApplication.java` exists in package `com.example.ticketbooking.event` and is annotated `@SpringBootApplication`.
    - `application.yml` contains `port: 8081`, `ddl-auto: validate`, and `flyway` enabled with `classpath:db/migration`.
    - `application.yml` references credentials via env-var placeholders (`${DB_USERNAME:` and `${DB_PASSWORD:`), not hardcoded plaintext.
    - `docker-compose.yml` exists at project root with a postgres service and database `ticketbooking`.
    - `./mvnw clean install -DskipTests` exits 0, building both the parent and event-service modules.
  </acceptance_criteria>
  <done>Both modules compile via the Maven wrapper; the Spring Boot application class, application.yml (port 8081, ddl-auto validate, Flyway enabled), and docker-compose.yml exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application config → database | Datasource credentials cross from config into the JDBC connection |
| build → Maven Central | Dependency resolution pulls third-party JARs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | application.yml datasource credentials | mitigate | Reference credentials via env-var placeholders `${DB_USERNAME}` / `${DB_PASSWORD}` with local-only defaults; never commit production secrets |
| T-01-02 | Tampering | Maven dependency supply chain | accept | All packages are industry-standard from Maven Central per RESEARCH.md Package Legitimacy Audit (all [ASSUMED]/[CITED], no [SUS]/[SLOP]); no install-time checkpoint required for established Java ecosystem packages |
| T-01-SC | Tampering | Maven dependency installs | accept | RESEARCH.md audit verdicts are all Approved; no [ASSUMED]/[SUS] package needs a blocking human-verify gate in this phase |
</threat_model>

<verification>
- `./mvnw clean install -DskipTests` succeeds from the project root, building parent + event-service.
- Parent POM has `pom` packaging; event-service is the only module with `spring-boot-maven-plugin`.
- Annotation processor ordering is Lombok → mapstruct-processor → lombok-mapstruct-binding.
- `application.yml` sets port 8081, `ddl-auto: validate`, and Flyway enabled.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 partially satisfied: `mvn clean install` compiles parent POM and event-service module without errors (full Flyway/entity validation completes in Plan 02).
- INFR-01 satisfied: multi-module Maven structure with parent POM and event-service module exists and builds.
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold-data-layer/01-01-SUMMARY.md` when done.
</output>
