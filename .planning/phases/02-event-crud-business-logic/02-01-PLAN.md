---
phase: 02-event-crud-business-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - event-service/src/main/resources/db/migration/V6__update_event_status_default_to_draft.sql
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/EventStatus.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/repository/EventRepository.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/specification/EventSpecification.java
  - event-service/src/main/resources/application.yml
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/PagedResponse.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreateEventRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateEventRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreateVenueRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateVenueRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreatePerformerRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdatePerformerRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreateTicketRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateTicketRequest.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/EventMapper.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/VenueMapper.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/PerformerMapper.java
  - event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/TicketMapper.java
autonomous: true
requirements:
  - EVNT-06
  - EVNT-07
  - EVNT-09
  - EVNT-10

must_haves:
  truths:
    - "EventStatus enum contains DRAFT as a valid value alongside UPCOMING, ONGOING, COMPLETED, CANCELLED"
    - "EventRepository supports JPA Specification queries via JpaSpecificationExecutor"
    - "EventSpecification provides five composable static filter predicates (category, city, status, dateFrom, dateTo)"
    - "All four MapStruct mappers have partial-update methods that skip null source fields"
    - "UpdateEventRequest has no status field — transitions are enforced via action endpoints only"
    - "PagedResponse generic wrapper can be constructed from any Spring Page via static factory"
    - "Jackson serializes all DTO fields as snake_case JSON"
  artifacts:
    - path: "event-service/src/main/resources/db/migration/V6__update_event_status_default_to_draft.sql"
      provides: "Changes events.status column default from UPCOMING to DRAFT"
    - path: "event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/EventStatus.java"
      provides: "EventStatus enum with DRAFT, UPCOMING, ONGOING, COMPLETED, CANCELLED"
      contains: "DRAFT"
    - path: "event-service/src/main/java/com/kobukuro/ticketbooking/event/specification/EventSpecification.java"
      provides: "Composable Specification<Event> predicates for filtering"
      exports: ["hasCategory", "hasCity", "hasStatus", "eventDateAfter", "eventDateBefore"]
    - path: "event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/PagedResponse.java"
      provides: "Generic pagination wrapper record with static of(Page<T>) factory"
    - path: "event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateEventRequest.java"
      provides: "PATCH request DTO with nullable fields and no EventStatus field (per D-16)"
    - path: "event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/EventMapper.java"
      provides: "MapStruct mapper with updateEventFromRequest(@MappingTarget) partial-update method"
  key_links:
    - from: "EventSpecification"
      to: "EventRepository"
      via: "JpaSpecificationExecutor.findAll(Specification<Event>, Pageable) — added to EventRepository"
    - from: "EventMapper.updateEventFromRequest"
      to: "Event entity"
      via: "MapStruct @BeanMapping(nullValuePropertyMappingStrategy=IGNORE) + @MappingTarget — generated at compile time"
    - from: "PagedResponse.of"
      to: "Page<T>"
      via: "Static factory delegating to page.getContent(), getTotalPages(), getTotalElements(), getNumber(), getSize()"
---

<objective>
Establish the shared infrastructure that Plans 02-02, 02-03, and 02-04 depend on: DRAFT status added to the event state machine via a Flyway migration and enum update, dynamic JPA filtering enabled via Specification infrastructure, request DTOs created for all four resource families, MapStruct mappers extended with null-property-ignoring partial-update methods, and Jackson configured for snake_case serialization.

Purpose: Wave 1 gate — all service and controller plans are blocked until this plan compiles cleanly.
Output: V6 migration, updated EventStatus enum, EventRepository with JpaSpecificationExecutor, EventSpecification utility class, 8 request DTOs, PagedResponse record, 4 updated MapStruct mappers, application.yml with SNAKE_CASE Jackson config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-event-crud-business-logic/02-CONTEXT.md
@.planning/phases/02-event-crud-business-logic/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB Migration, Enum, Repository Extension, EventSpecification, Jackson Config</name>
  <files>
    event-service/src/main/resources/db/migration/V6__update_event_status_default_to_draft.sql,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/EventStatus.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/repository/EventRepository.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/specification/EventSpecification.java,
    event-service/src/main/resources/application.yml
  </files>
  <read_first>
    event-service/src/main/resources/db/migration/V3__create_events_table.sql (confirms no CHECK constraint on status — only the column default needs changing)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/EventStatus.java (current enum constants to preserve)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/repository/EventRepository.java (current interface — add to extends clause without removing JpaRepository)
    event-service/src/main/resources/application.yml (existing structure — append jackson section under spring)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/Event.java (field names used in Specification predicates: "category", "status", "eventDateTime", "venue")
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/Venue.java (confirms field name "city" used in hasCity join path)
    .planning/phases/02-event-crud-business-logic/02-CONTEXT.md (D-01: DRAFT added; D-10: filter params; D-12: default sort)
  </read_first>
  <action>
    Create V6__update_event_status_default_to_draft.sql in the db/migration directory. The file contains one SQL statement: ALTER TABLE events ALTER COLUMN status SET DEFAULT followed by the string literal DRAFT. No CHECK constraint modification is needed — V3 confirms no constraint exists on the status column.

    Update EventStatus.java: insert DRAFT as the first constant in the enum body, before UPCOMING. The complete ordered enum constant list becomes DRAFT, UPCOMING, ONGOING, COMPLETED, CANCELLED. Preserve the package declaration and class structure. The existing four constants must not be renamed or removed.

    Update EventRepository.java: change the interface declaration from extending only JpaRepository<Event, UUID> to extending both JpaRepository<Event, UUID> and JpaSpecificationExecutor<Event>. Add import org.springframework.data.jpa.repository.JpaSpecificationExecutor at the top of the file. No other changes.

    Create EventSpecification.java in a new package com.kobukuro.ticketbooking.event.specification (create the directory). Declare the class as public final and add a private no-arg constructor to prevent instantiation. Import jakarta.persistence.criteria.JoinType. Declare five public static Specification<Event> methods:

    hasCategory(EventCategory category): the lambda returns null when category is null; otherwise returns cb.equal(root.get("category"), category). The string "category" matches the field name in Event.java.

    hasCity(String city): the lambda returns null when city is null; otherwise creates a Join via root.join("venue", JoinType.INNER) — the string "venue" matches the @ManyToOne field name in Event.java — then returns cb.equal applied to the join's "city" attribute — the string "city" matches the field in Venue.java.

    hasStatus(EventStatus status): the lambda returns null when status is null; otherwise returns cb.equal(root.get("status"), status). The string "status" matches the @Column field in Event.java.

    eventDateAfter(LocalDateTime from): the lambda returns null when from is null; otherwise returns cb.greaterThanOrEqualTo(root.get("eventDateTime"), from). The string "eventDateTime" matches the field name in Event.java.

    eventDateBefore(LocalDateTime to): the lambda returns null when to is null; otherwise returns cb.lessThanOrEqualTo(root.get("eventDateTime"), to).

    Update application.yml: add a new jackson sub-key under the existing spring key. Under spring.jackson add: property-naming-strategy: SNAKE_CASE. This enables global snake_case serialization for all Jackson-serialized objects without per-class annotations.
  </action>
  <acceptance_criteria>
    - EventStatus.java has five enum constants in order: DRAFT first, then UPCOMING, ONGOING, COMPLETED, CANCELLED
    - EventRepository interface declaration contains JpaSpecificationExecutor in the extends clause
    - EventSpecification.java is a final class in the specification package with exactly five public static methods named hasCategory, hasCity, hasStatus, eventDateAfter, eventDateBefore
    - hasCity implementation joins using JoinType.INNER with the path string "venue"
    - application.yml contains the line "property-naming-strategy: SNAKE_CASE" nested under spring.jackson
    - V6 migration SQL file exists with an ALTER TABLE events statement that sets the status column default to DRAFT
    - mvn compile -pl event-service exits 0 (no compilation errors)
  </acceptance_criteria>
  <verify>
    <automated>mvn compile -pl event-service</automated>
  </verify>
  <done>Compilation passes; DRAFT is a valid EventStatus constant; EventRepository supports Specification queries; EventSpecification provides all 5 predicates; application.yml enables snake_case JSON serialization.</done>
</task>

<task type="auto">
  <name>Task 2: Request DTOs, PagedResponse, and Mapper Partial-Update Methods</name>
  <files>
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/PagedResponse.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreateEventRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateEventRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreateVenueRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateVenueRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreatePerformerRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdatePerformerRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/CreateTicketRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/dto/UpdateTicketRequest.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/EventMapper.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/VenueMapper.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/PerformerMapper.java,
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/TicketMapper.java
  </files>
  <read_first>
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/Event.java (exact field names for @Mapping ignore targets: id, status, performers, venue, createdAt, updatedAt)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/Venue.java (ignore targets: id, createdAt, updatedAt)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/Performer.java (ignore targets: id, createdAt, updatedAt)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/Ticket.java (ignore targets: id, event, createdAt, updatedAt)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/EventMapper.java (preserve existing toDto and toEntity methods)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/VenueMapper.java (preserve existing methods)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/PerformerMapper.java (preserve existing methods)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/mapper/TicketMapper.java (preserve existing @Mapping annotations on toDto and toEntity)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/EventCategory.java (type used in CreateEventRequest and UpdateEventRequest)
    event-service/src/main/java/com/kobukuro/ticketbooking/event/entity/TicketStatus.java (type used in UpdateTicketRequest)
    .planning/phases/02-event-crud-business-logic/02-CONTEXT.md (D-07: venueId + performerIds in create; D-13: PATCH partial; D-15: venueId in PATCH; D-16: no EventStatus field in UpdateEventRequest)
  </read_first>
  <action>
    Create PagedResponse.java in the dto package as a Java generic record: public record PagedResponse<T> with components List<T> content, int totalPages, long totalElements, int number, int size. Inside the record body, add a public static generic method named "of" that accepts a Page<T> parameter and returns PagedResponse<T> by constructing from page.getContent(), page.getTotalPages(), page.getTotalElements(), page.getNumber(), page.getSize(). Import org.springframework.data.domain.Page and java.util.List.

    Create CreateEventRequest.java in the dto package as a Java record with components: String name, String description, EventCategory category, UUID venueId, List<UUID> performerIds, LocalDateTime eventDateTime. Import EventCategory, UUID, List, LocalDateTime. This implements D-07 — venueId and performerIds are accepted in the create body.

    Create UpdateEventRequest.java in the dto package as a regular mutable class (not a record — null means "field not provided, skip"). Annotate with Lombok @Getter, @Setter, @NoArgsConstructor. Declare these fields with their types: String name, String description, EventCategory category, UUID venueId, LocalDateTime eventDateTime. This class must not declare any field of type EventStatus or any field that represents a transition signal — those are handled exclusively by action endpoints per D-16. The venueId field is a UUID per D-15; the service layer resolves it to a Venue entity.

    Create CreateVenueRequest.java in the dto package as a Java record with components: String name, String country, String city, String address.

    Create UpdateVenueRequest.java in the dto package as a @Getter @Setter @NoArgsConstructor class with nullable fields: String name, String country, String city, String address.

    Create CreatePerformerRequest.java in the dto package as a Java record with component: String name.

    Create UpdatePerformerRequest.java in the dto package as a @Getter @Setter @NoArgsConstructor class with nullable field: String name.

    Create CreateTicketRequest.java in the dto package as a Java record with components: String seatNumber, BigDecimal price. seatNumber may be null (optional seat assignment). Import java.math.BigDecimal.

    Create UpdateTicketRequest.java in the dto package as a @Getter @Setter @NoArgsConstructor class with nullable fields: String seatNumber, BigDecimal price, TicketStatus status. Import TicketStatus.

    Update EventMapper.java: add a third method to the existing interface after the toEntity method. The method signature is void updateEventFromRequest(UpdateEventRequest request, @MappingTarget Event event). Annotate with @BeanMapping(nullValuePropertyMappingStrategy = NullValuePropertyMappingStrategy.IGNORE). Add six @Mapping annotations each with ignore = true targeting: id, status, performers, venue, createdAt, updatedAt — these entity fields must not be touched by the partial-update method. Add all required MapStruct imports: org.mapstruct.BeanMapping, org.mapstruct.MappingTarget, org.mapstruct.NullValuePropertyMappingStrategy, org.mapstruct.Mapping. Preserve the existing toDto(Event) and toEntity(EventDto) methods unchanged.

    Update VenueMapper.java: add a third method void updateVenueFromRequest(UpdateVenueRequest request, @MappingTarget Venue venue). Same @BeanMapping IGNORE strategy. Ignore targets: id, createdAt, updatedAt.

    Update PerformerMapper.java: add a third method void updatePerformerFromRequest(UpdatePerformerRequest request, @MappingTarget Performer performer). Same @BeanMapping IGNORE strategy. Ignore targets: id, createdAt, updatedAt.

    Update TicketMapper.java: add a third method void updateTicketFromRequest(UpdateTicketRequest request, @MappingTarget Ticket ticket). Same @BeanMapping IGNORE strategy. Ignore targets: id, event, createdAt, updatedAt. The existing @Mapping(source = "event.id", target = "eventId") on toDto and @Mapping(source = "eventId", target = "event.id") on toEntity must be preserved exactly.
  </action>
  <acceptance_criteria>
    - PagedResponse.java is a public generic record with a static of(Page<T>) factory method delegating to all five Page methods
    - CreateEventRequest.java is a Java record with exactly six components: name, description, category, venueId, performerIds, eventDateTime (per D-07)
    - UpdateEventRequest.java is a Lombok class (@Getter @Setter @NoArgsConstructor) with exactly five fields: name, description, category, venueId, eventDateTime — no field with type EventStatus exists in this file (per D-16)
    - All four UpdateXxx request classes are mutable (class with setters), not records
    - All four mappers compile after adding the updateXxxFromRequest method (MapStruct generates implementations)
    - EventMapper.updateEventFromRequest has @Mapping(target = "status", ignore = true) and @Mapping(target = "venue", ignore = true)
    - TicketMapper preserves its existing @Mapping(source = "event.id", target = "eventId") on toDto
    - mvn compile -pl event-service exits 0 with no unmapped-mandatory-property MapStruct errors
  </acceptance_criteria>
  <verify>
    <automated>mvn compile -pl event-service</automated>
  </verify>
  <done>All 9 DTO/wrapper files created in the dto package; all 4 mappers extended with partial-update methods; mvn compile passes with 0 errors; MapStruct annotation processor generates concrete implementations for all updateXxxFromRequest methods.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API request → UpdateEventRequest | User-supplied JSON is bound to UpdateEventRequest; only declared fields can be set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | UpdateEventRequest | mitigate | Separate request DTO prevents direct entity binding; EventStatus field absent from UpdateEventRequest per D-16 — users cannot inject status transitions via PATCH |
| T-02-02 | Tampering | EventMapper.updateEventFromRequest | mitigate | @Mapping(target="status", ignore=true) on the MapStruct partial-update method provides compile-time enforcement that status is never overwritten via PATCH, independent of UpdateEventRequest shape |
| T-02-03 | Tampering | All UpdateXxx request DTOs | mitigate | @BeanMapping(nullValuePropertyMappingStrategy=IGNORE) ensures only non-null fields overwrite entity state; null means "unchanged", preventing accidental data erasure |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies audited in Phase 1 |
</threat_model>

<verification>
Run from project root after both tasks complete:
- mvn compile -pl event-service must exit 0
- mvn test -pl event-service must exit 0 (existing EventDataLayerIT still passes — adding DRAFT to EventStatus is backward-compatible; existing tests use UPCOMING which is still valid)
</verification>

<success_criteria>
- EventStatus has DRAFT as a valid constant
- EventRepository.findAll(Specification, Pageable) compiles and is callable
- EventSpecification.hasCategory, hasCity, hasStatus, eventDateAfter, eventDateBefore all return nullable Specification<Event>
- All 8 request DTOs exist and compile
- PagedResponse<T> generic record exists with static of() factory
- All 4 mappers have updateXxxFromRequest(@MappingTarget) with NullValuePropertyMappingStrategy.IGNORE
- UpdateEventRequest has no EventStatus field (enforced by D-16)
- application.yml has spring.jackson.property-naming-strategy: SNAKE_CASE
</success_criteria>

## Artifacts This Plan Produces

| Symbol | Kind | File |
|--------|------|------|
| EventStatus.DRAFT | enum constant | entity/EventStatus.java |
| V6__update_event_status_default_to_draft.sql | Flyway migration | db/migration/ |
| EventRepository extends JpaSpecificationExecutor | interface extension | repository/EventRepository.java |
| EventSpecification | final utility class | specification/EventSpecification.java |
| EventSpecification.hasCategory | static method | specification/EventSpecification.java |
| EventSpecification.hasCity | static method | specification/EventSpecification.java |
| EventSpecification.hasStatus | static method | specification/EventSpecification.java |
| EventSpecification.eventDateAfter | static method | specification/EventSpecification.java |
| EventSpecification.eventDateBefore | static method | specification/EventSpecification.java |
| PagedResponse<T> | record | dto/PagedResponse.java |
| PagedResponse.of(Page<T>) | static factory | dto/PagedResponse.java |
| CreateEventRequest | record | dto/CreateEventRequest.java |
| UpdateEventRequest | class | dto/UpdateEventRequest.java |
| CreateVenueRequest | record | dto/CreateVenueRequest.java |
| UpdateVenueRequest | class | dto/UpdateVenueRequest.java |
| CreatePerformerRequest | record | dto/CreatePerformerRequest.java |
| UpdatePerformerRequest | class | dto/UpdatePerformerRequest.java |
| CreateTicketRequest | record | dto/CreateTicketRequest.java |
| UpdateTicketRequest | class | dto/UpdateTicketRequest.java |
| EventMapper.updateEventFromRequest | interface method | mapper/EventMapper.java |
| VenueMapper.updateVenueFromRequest | interface method | mapper/VenueMapper.java |
| PerformerMapper.updatePerformerFromRequest | interface method | mapper/PerformerMapper.java |
| TicketMapper.updateTicketFromRequest | interface method | mapper/TicketMapper.java |

<output>
Create `.planning/phases/02-event-crud-business-logic/02-01-SUMMARY.md` when done.
</output>
